<!DOCTYPE html>
<html>
<head>
    <title>{{ article.title }}</title>
    <meta charset="utf-8" />
    <meta property="og:type" content="article">
    <meta property="og:title" content="{{ article.title | escape }}">
    <meta property="og:url" content="{{ s3_url }}">
    {% if article.top_image | length > 0 -%}
    <meta property="og:image" content="{{ article.top_image }}">
    {%- endif %}
    {% if article.meta_description | length > 0 -%}
    <meta property="og:description" content="{{ article.meta_description }}">
    {%- endif %}
    {% if article.authors | length > 0 -%}
    <meta property="article:author" content="{{ article.authors[0] }}">
    {%- endif %}
    {% if published_date_iso | length > 0 -%}
    <meta property="article:published_time" content="{{ published_date_iso }}">
    {%- endif %}
    <link rel="stylesheet" type="text/css" href="//fonts.googleapis.com/css?family=Open+Sans" />
    <style>
    h1 { font-family: "Open Sans"; font-size: 24px; font-style: normal; font-variant: normal; font-weight: 700; line-height: 26.4px; } h3 { font-family: "Open Sans"; font-size: 14px; font-style: normal; font-variant: normal; font-weight: 700; line-height: 15.4px; } p { font-family: "Open Sans"; font-size: 14px; font-style: normal; font-variant: normal; font-weight: 400; line-height: 20px; } blockquote { font-family: "Open Sans"; font-size: 21px; font-style: normal; font-variant: normal; font-weight: 400; line-height: 30px; } pre { font-family: "Open Sans"; font-size: 13px; font-style: normal; font-variant: normal; font-weight: 400; line-height: 18.5714px; }
    </style>
</head>
<body>
<div class="header">
  <p>{{ website }} | <a href="{{ base_name }}.png" target="_blank">screenshot</a> | <a href="{{ url }}" target="_blank">source</a> | <a href="{{ base_name }}.pdf" target="_blank">pdf</a> (<a href="https://youtu.be/ADlGlFGAdiY" target="_blank">?</a>) | parsed: {{ parsed_on }}</p>
</div>
<div class="content">
  <h1>{{ article.title }}</h1>
  <p><strong>by:</strong> {{ authors_csv }}</p>
  <p><em>on {{ publish_date_str }}</em></p>
{% for p in article_paragraphs %}
  <p>{{ p }}</p>
{%- endfor %}
</div>
</body>
</html>